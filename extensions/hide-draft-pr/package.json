{
	"name": "hide-draft-pr",
	"version": "1.0.0",
	"private": true,
	"description": "Hide draft pull requests on GitHub PR list",
	"scripts": {
		"dev": "wxt",
		"build": "wxt build && cp -r .output/chrome-mv3/* ~/.chrome/extensions/hide-draft-pr/",
		"postinstall": "wxt prepare"
	},
	"devDependencies": {
		"typescript": "^5.7.0",
		"wxt": "^0.19.0"
	}
}
