{
	"name": "copy-tweet",
	"version": "1.0.0",
	"private": true,
	"description": "Copy tweet and article content as Markdown with embedded images",
	"scripts": {
		"dev": "wxt",
		"build": "wxt build && cp -r .output/chrome-mv3/* ~/.chrome/extensions/copy-tweet/",
		"postinstall": "wxt prepare"
	},
	"devDependencies": {
		"typescript": "^5.7.0",
		"wxt": "^0.19.0"
	}
}
