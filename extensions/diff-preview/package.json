{
	"name": "diff-preview",
	"version": "1.0.0",
	"private": true,
	"description": "Preview the rendered output of HTML files on GitHub PR Files Changed tab",
	"scripts": {
		"dev": "wxt",
		"build": "wxt build && cp -r .output/chrome-mv3/* ~/.chrome/extensions/diff-preview/",
		"postinstall": "wxt prepare"
	},
	"devDependencies": {
		"typescript": "^5.7.0",
		"wxt": "^0.19.0"
	}
}
